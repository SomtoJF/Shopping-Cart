import ProductsHeader from "../Components/Header";
import "../styles/products/Products.sass";

const Products = ({ products }) => {
	return <ProductsHeader text="MINIMALIST COLLECTIONS" />;
};

export default Products;
